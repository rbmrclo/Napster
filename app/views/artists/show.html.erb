<ul class="nav nav-list">
<li class="active"><i class="icon-home icon-white"></i><%= link_to "Home", root_path %></li>
</ul>
<div class="dialog">
<h1><%= @artist.name.titlecase %></h1>
<li><b>Bio: </b><%= @artist.bio %> </li>
<li><b><u><%= link_to 'Albums', artist_album_path(@artist) %></u></b></li>
<ol type="1">
<% @artist.albums.each do |album| %>
<li class="text-warning"><%= album.album_name %></li>
<% end %>
</ol>
</div>
<br/>
<%= link_to 'Add album', new_artist_album_path(@artist) %>
<hr>
<%= link_to 'Back to Artists', artists_path %>